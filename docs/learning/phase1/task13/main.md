# Phase 1 / Task 13: Frontend — 設定ページ (AtCoderユーザー名登録)

## 概要

`/settings` で AtCoder のユーザー名を登録 / 更新する。Backend `POST /users/me` を呼ぶ。

主な学習ポイント:
- React フォームの基本 (`useState`, `onSubmit`)
- `try/catch/finally` での loading 状態管理
- 二重送信防止 (`disabled`)

## 目次

- 共通教材を参照: [Task 12 / 01 - Next.js client page パターン](../task12/01-nextjs-client-page-with-auth-and-api.md) §E (フォーム + 副作用)

## 振り返り

（Task 内の全 Topic クイズ完了後に生成）
